---
phase: 07-teams-users
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/users/users.go
  - cmd/users/list.go
  - cmd/users/get.go
  - cmd/users/create.go
  - cmd/users/update.go
  - cmd/users/delete.go
  - cmd/users/list_test.go
  - cmd/users/get_test.go
  - cmd/users/create_test.go
  - cmd/users/update_test.go
  - cmd/users/delete_test.go
  - main.go
autonomous: true
requirements:
  - USER-01
  - USER-02
  - USER-03
  - USER-04
  - USER-05

must_haves:
  truths:
    - "revenium users list displays users in a styled table with ID, Email, Name, Roles"
    - "revenium users get <id> displays a single user"
    - "revenium users create --email <e> --first-name <f> --last-name <l> --roles <r> --team-ids <t> creates a user"
    - "revenium users update <id> --first-name <name> updates and renders the user"
    - "revenium users delete <id> prompts for confirmation and deletes"
    - "All user commands support --json output"
    - "Empty user list prints 'No users found.' in text mode"
  artifacts:
    - path: "cmd/users/users.go"
      provides: "Parent command, tableDef, toRows, str, renderUser"
      exports: ["Cmd"]
    - path: "cmd/users/list.go"
      provides: "List users command"
    - path: "cmd/users/get.go"
      provides: "Get user by ID command"
    - path: "cmd/users/create.go"
      provides: "Create user command with required slice flags"
    - path: "cmd/users/update.go"
      provides: "Update user command"
    - path: "cmd/users/delete.go"
      provides: "Delete user with confirmation"
    - path: "main.go"
      provides: "Users registration"
      contains: "cmd.RegisterCommand(users.Cmd"
  key_links:
    - from: "cmd/users/*.go"
      to: "cmd.APIClient"
      via: "Do() method calls"
      pattern: 'cmd\.APIClient\.Do.*"/v2/api/users'
    - from: "cmd/users/users.go"
      to: "internal/output"
      via: "TableDef and Render"
      pattern: "output\\.TableDef"
    - from: "cmd/users/delete.go"
      to: "internal/resource"
      via: "ConfirmDelete helper"
      pattern: "resource\\.ConfirmDelete"
    - from: "main.go"
      to: "cmd/users"
      via: "RegisterCommand"
      pattern: 'RegisterCommand\(users\.Cmd'
---

<objective>
Implement full CRUD for the Users resource, following the established products pattern.

Purpose: Users can manage user accounts from the CLI (USER-01 through USER-05).
Output: Complete cmd/users/ package with all commands, tests, and main.go registration.
</objective>

<execution_context>
@/Users/johndemic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johndemic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-teams-users/07-RESEARCH.md

@cmd/products/products.go
@cmd/products/list.go
@cmd/products/get.go
@cmd/products/create.go
@cmd/products/update.go
@cmd/products/delete.go
@cmd/products/delete_test.go
@cmd/products/list_test.go
@cmd/subscribers/subscribers.go
@cmd/subscribers/create.go
@main.go

<interfaces>
<!-- Executor needs these existing contracts. No codebase exploration needed. -->

From cmd/root.go (via cmd package):
```go
var APIClient *api.Client
var Output *output.Formatter
func RegisterCommand(cmd *cobra.Command, group string)
func Execute() error
```

From internal/output:
```go
type TableDef struct {
    Headers      []string
    StatusColumn int  // 0-indexed, -1 if no status column
}
func (f *Formatter) Render(def TableDef, rows [][]string, jsonData interface{}) error
func (f *Formatter) RenderJSON(data interface{}) error
func (f *Formatter) IsJSON() bool
func (f *Formatter) IsQuiet() bool
func NewWithWriter(out, errOut io.Writer, json, quiet bool) *Formatter
```

From internal/resource:
```go
func ConfirmDelete(resourceType, id string, yes, jsonMode bool) (bool, error)
```

From internal/api:
```go
func NewClient(baseURL, apiKey string, verbose bool) *Client
func (c *Client) Do(ctx context.Context, method, path string, body, result interface{}) error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create users package with all CRUD commands</name>
  <files>cmd/users/users.go, cmd/users/list.go, cmd/users/get.go, cmd/users/create.go, cmd/users/update.go, cmd/users/delete.go</files>
  <action>
Clone the cmd/products/ package structure for users. Users have more fields and require string slice flags for roles and team-ids. Specific details:

**users.go** -- Parent command + helpers:
- Package: `users`
- Cmd Use: "users", Short: "Manage users"
- Example: list, get, create examples using "revenium users"
- tableDef: Headers `["ID", "Email", "Name", "Roles"]`, StatusColumn: -1
- toRows: extract "id", "email", compose name from "firstName" + " " + "lastName" (use strings.TrimSpace like subscribers pattern), format "roles" array by joining with ", " (type-assert to []interface{} then fmt.Sprint each element)
- str(): identical helper (each package has its own per convention)
- rolesStr(m map[string]interface{}) string: extract "roles" field, type-assert to []interface{}, join elements with ", " using fmt.Sprint on each. Return "" if nil/missing.
- renderUser(): single-row render using same 4 columns
- Import "strings" for TrimSpace and Join

**list.go** -- newListCmd():
- API: GET /v2/api/users
- Empty: "No users found." in text, empty array in JSON
- 2 examples: basic list, list with --json

**get.go** -- newGetCmd():
- Args: cobra.ExactArgs(1)
- API: GET /v2/api/users/{id}
- Renders single user via renderUser()
- 2 examples: basic get, get with --json

**create.go** -- newCreateCmd():
- Required flags: --email (string), --first-name (string), --last-name (string), --roles (StringSliceVar []string), --team-ids (StringSliceVar []string)
- Optional flags: --phone-number (string), --can-view-prompt-data (bool)
- Body: always include "email", "firstName", "lastName", "roles", "teamIds"; include optional fields only if Flags().Changed()
- IMPORTANT: Use StringSliceVar (not StringVar) for --roles and --team-ids. Cobra supports comma-separated: --roles ROLE_API_CONSUMER,ROLE_ADMIN
- Mark all 5 required flags via MarkFlagRequired
- API: POST /v2/api/users
- Renders result via renderUser()
- 2 examples: minimal create (required only), create with optional fields

**update.go** -- newUpdateCmd():
- Args: cobra.ExactArgs(1)
- All flags optional: --email, --first-name, --last-name, --roles (StringSliceVar), --team-ids (StringSliceVar), --phone-number, --can-view-prompt-data (bool)
- Body: only include changed flags via Flags().Changed(). Map flag names: "first-name" -> "firstName", "last-name" -> "lastName", "team-ids" -> "teamIds", "phone-number" -> "phoneNumber", "can-view-prompt-data" -> "canViewPromptData"
- Error if no fields specified: "no fields specified to update"
- API: PUT /v2/api/users/{id}
- Renders result via renderUser()
- 2 examples: update email, update roles

**delete.go** -- newDeleteCmd():
- Clone from cmd/products/delete.go
- Change: "product" -> "user", API path -> "/v2/api/users/"+id
- Uses resource.ConfirmDelete("user", id, yes, cmd.Output.IsJSON())
- 2 examples: delete with confirmation, delete with --yes
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/revenium-cli && go build ./cmd/users/...</automated>
  </verify>
  <done>All 6 Go source files in cmd/users/ compile without errors. Package exports Cmd with 5 subcommands.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests and register users in main.go</name>
  <files>cmd/users/list_test.go, cmd/users/get_test.go, cmd/users/create_test.go, cmd/users/update_test.go, cmd/users/delete_test.go, main.go</files>
  <action>
**Tests** -- Clone test patterns from cmd/products/ test files.

Mock API response for users: `{"id": "user-1", "email": "jane@example.com", "firstName": "Jane", "lastName": "Doe", "roles": ["ROLE_API_CONSUMER"], "teamIds": ["team-1"], "resourceType": "user"}`

**list_test.go:**
- TestListUsers: returns array of 1 user, assert output contains "user-1", "jane@example.com", "Jane Doe", "ROLE_API_CONSUMER"
- TestListUsersEmpty: returns `[]`, assert output contains "No users found."
- TestListUsersJSON: json=true, assert output contains `"user-1"`
- TestListUsersEmptyJSON: json=true, returns `[]`, assert output contains `[]`

**get_test.go:**
- TestGetUser: returns single user, assert contains "user-1", "jane@example.com"
- TestGetUserJSON: json=true, assert JSON output

**create_test.go:**
- TestCreateUser: --email "jane@example.com" --first-name "Jane" --last-name "Doe" --roles "ROLE_API_CONSUMER" --team-ids "team-1", assert POST to /v2/api/users, assert request body contains "email", "firstName", "lastName", "roles" (as array), "teamIds" (as array), assert output contains "user-1"
- TestCreateUserWithOptional: add --phone-number "555-1234" --can-view-prompt-data, assert body contains "phoneNumber" and "canViewPromptData"

**update_test.go:**
- TestUpdateUser: --email "new@example.com", assert PUT to /v2/api/users/user-1, assert body contains "email"
- TestUpdateUserNoFields: no flags, assert error "no fields specified to update"

**delete_test.go:**
- TestDeleteUserWithYes: --yes flag, assert DELETE called, output contains "Deleted user user-1."
- TestDeleteUserQuiet: quiet=true, assert empty output
- TestDeleteUserJSONMode: json=true, assert delete proceeds without prompt

IMPORTANT for delete tests: Register `c.Flags().Bool("yes", false, "Skip confirmation prompts")` on the test command before execution.

**main.go** -- Add import `"github.com/revenium/revenium-cli/cmd/users"` and add `cmd.RegisterCommand(users.Cmd, "resources")` in init() after the teams registration. Read main.go first to see current registrations -- if plan 07-01 already added teams, add users alongside. If running first, add users only.

NOTE: Both plans 07-01 and 07-02 touch main.go. The executor should read main.go first and add only what's missing.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/revenium-cli && go test ./cmd/users/... -v -count=1</automated>
  </verify>
  <done>All user tests pass. `go test ./...` remains green. Users command appears in CLI help via main.go registration.</done>
</task>

</tasks>

<verification>
- `go test ./cmd/users/... -v` -- all tests pass
- `go build -o /dev/null .` -- full binary compiles
- `go test ./...` -- no regressions in other packages
</verification>

<success_criteria>
- cmd/users/ package has 6 source files and 5 test files
- All CRUD operations (list, get, create, update, delete) work with correct API paths
- Users registered in main.go and visible in CLI help
- All tests pass including JSON mode, empty list, quiet mode, and delete confirmation variants
- String slice flags (--roles, --team-ids) correctly send arrays to the API
</success_criteria>

<output>
After completion, create `.planning/phases/07-teams-users/07-02-SUMMARY.md`
</output>
